export enum ENV {
  ENV = 'ENV',
  PORT = 'PORT',
  VERSION = 'VERSION',
  THROTTLE_TTL = 'THROTTLE_TTL', // Time to live in milliseconds
  THROTTLE_LIMIT = 'THROTTLE_LIMIT', // Maximum number of requests within the ttl
  AI_ENABLED = 'AI_ENABLED',
  AI_API_BASE_URL = 'AI_API_BASE_URL',
  AI_API_KEY = 'AI_API_KEY',
}
